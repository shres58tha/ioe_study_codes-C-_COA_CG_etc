#include <stdio.h>

int main() {
    int i=1;
    printf("%d %d %d %d %d %d %d %d %d %d\n",i , i+++i, i++, i++, i++, i++,i++, i++, i++, i++);
    if (i| printf("\nhello i love you so much \n"))
        printf("\nwhats going on ???? %d\n",i);
    
    if ( i || printf("\nhello my love for you is hidden \n"))
        printf("\nwhats going on ???? %d\n",i);
return 0;
}
