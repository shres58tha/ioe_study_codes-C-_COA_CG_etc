#/* \
echo "hello world from shell." && make compile 2>/dev/null; exit \

default:
	@echo hello, world from make.
compile:
	@gcc -xc Makefile
	@false
#*/
#include <stdio.h>
	const char *what = "hello, world from c."; \
        int main(int argc, char *argv[]) \
        { \
                (void)argc; \
                (void)argv; \
                printf("%s\n", what); \
                return 0; \
        }
