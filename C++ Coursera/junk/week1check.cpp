#include <iostream>
#include <vector>

using namespace std;

   const int N=40;
   template <type name I>
   Int sum (T value)
{
    intS=0;
    for (int i+0; i<val,size(); ++i)
    S+=val[i];
    return s;
}
    Int main()
{
    vector<int>vec;
    for(int i=0; i<N; ++1)
       vec.push_back(i);
    count<<" sum is "<<(vec) '\n';
    return 0;
}
